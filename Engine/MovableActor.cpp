#include "pch.h"
#include "MovableActor.h"

void MovableActor::Update(float deltaTime)
{
	Super::Update(deltaTime);
}

void MovableActor::Render(HDC hdc)
{
	Super::Render(hdc);
}

void MovableActor::Move(float deltaTime)
{
}
